{
  "private": true,
  "scripts": {
    "build": "spago build",
    "test": "spago -x ./test.dhall test",
    "pulp-build": "pulp build",
    "pulp-test": "pulp test"
  },
  "devDependencies": {
    "pulp": "^16.0.0",
    "purescript": "^0.15.4",
    "purescript-psa": "^0.6.0",
    "spago": "^0.20.9"
  }
}
